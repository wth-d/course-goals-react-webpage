// imports from third-party libraries (just the library name)
import React from 'react';
import ReactDOM from 'react-dom';

// imports from our own files (a relative path)
import './index.css';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));
